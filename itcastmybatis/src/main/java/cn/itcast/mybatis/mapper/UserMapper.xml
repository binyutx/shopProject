<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.itcast.mybatis.mapper.UserMapper">

    <select id="queryUserById" parameterType="long" resultType="User">
        SELECT * FROM user WHERE id=#{id}
    </select>
    <insert id="saveUser" useGeneratedKeys="true" keyColumn="id" keyProperty="id" >
        INSERT INTO user(user_name,password,name,age,sex,birthday,created,updated) VALUES (#{user_name},
        #{password},
        #{name},
        #{age},
        #{sex},
        #{birthday},
        #{created},
        #{updated})
    </insert>
    <update id="updateUserByid" >
        UPDATE user SET user_name=#{user_name},password=#{password},
        name=#{name},age=#{age},sex=#{sex},birthday=#{birthday},
        updated = now()
        WHERE
        (id=#{id})
    </update>
    <delete id="deleteUserById" >
        Delete FROM user WHERE id=#{id}
    </delete>
</mapper>